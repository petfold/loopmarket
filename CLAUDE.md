# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this project is

`loopmarket` is a **universal combinatorial marketplace**: every economic intention is one uniform, content-addressed **offer** (a thing described as a conjunction of OntoDAG categories, a service time window, a service region, a validity window — priced on the maker's **personal scale**, a pure-bookkeeping numeraire that the record encoding calls the maker's personal token: nothing is ever held or transferred, the numbers cancel in-loop); a distributed **offer book** holds them as a versioned recordstore keyspace (Swarm-backed in deployment); competing **solver agents** hunt profitable **loops** (cycles whose exchange-rate product exceeds 1 ⇔ negative cycles under −log weights — the P0 case; the general cleared object is a value-conserving **circulation** with composed legs, `docs/plans/P2-loop-selection.md` §11, reframed 2026-09-07); a **clearing** layer re-verifies every proposed loop from scratch and commits all its legs atomically. The full design rationale — including the parts deliberately not built yet — is in `ARCHITECTURE.md`. Read it before structural changes. The forward design corpus (P1–P4 work packages, threat register, catalogue bootstrap, factbond coupling) lives under `docs/plans/` — indexed on the front page, `README.md` (doc table, phase↔doc map, reading order); planned invariants U8–U14 are specified there and enter this file only when their enforcing code and tests land (U11 entered 2026-08-20).

The conceptual background is the "Loop Economy" essay (offers, loops, bridges, bonds, nano-bets, solver ecology). This repo is phase P0 of that programme: the in-memory/prototype track that must stay runnable end-to-end at all times.

## The stack and its direction

```
loopmarket  →  ontodag (>=0.26.1)  →  recordstore (>=0.20.3)  →  Swarm (optional)
```

- **ontodag** (github.com/petfold/ontodag): the shared catalogue. One query primitive — intersection of descendant cones, `get(terms, items_only=True)` (Peter, 2026-09-12: *ontodag is based on intersection*; a want is the wider cone, a give the narrower — an overlap query mode was built and withdrawn that day). `Ontology` in `src/loopmarket/ontology.py` is a thin matching-oriented facade (`covers`, `satisfies` over `OntoDAG.is_below`) and the place where pinned catalogue roots surface. Identity at ontodag's public boundary is the *name* (plain strings accepted anywhere an `Item` is). **Pin (2026-09-13):** `ontodag>=0.26.1` — the release that carries the graph kind (#19: `transport(small-item weight(..8kg))` as a term the graph orders), after 0.25.0's #14, #15, #16.
- **recordstore** (github.com/petfold/recordstore): the book's kernel. Canonical roots (equal content ⇒ equal reference), snapshot isolation via `RecordStore.at(root, blobs)` (note: the blob-store attribute is **`.blobs`**), three-way `merge`, `commit(reconcile=True, resolver=...)` for multi-writer convergence. The interface summary lives in ontodag's `docs/recordstore-interface.md`; treat it as the API reference.
- **Swarm**: reached only through recordstore (`BeeBytesStore` + `SwarmFeedPointer`, assembled by `swarm_store(topic, signer=/owner=)`). `registry.swarm_offer_book()` and `Ontology.persistent()` are the only places that name it.

## Dependency boundaries (tests/test_boundaries.py — must always pass)

- **B1 The core works offline.** `import loopmarket` and the whole model (schema, ontology, matching, graph, solver, mock clearing) must function with no network, no Bee node, and no optional dependency. Swarm is a persistence backend chosen at the edges, never a requirement of the model.
- **B2 One-directional dependencies.** loopmarket imports ontodag and recordstore; never the reverse, and never `requests`/`swarm_bee` at module import time (they load lazily inside the Swarm call paths, as in recordstore itself).

## Core invariants (do not weaken; add tests when touching them)

- **U1 Uniform offer form.** Exactly one side of every `Offer` is a `Thing`, exactly one is `Tokens`, and the token issuer **is** the maker. Enforced in `Offer.__post_init__`; the whole loop arithmetic depends on it.
- **U2 Offers are immutable, canonical values.** `canonical_bytes()` is deterministic (concept tuples sorted, recordstore's canonical JSON); `offer_id` = SHA-256 of it. Never add a field without bumping the record's `"v"` and keeping `from_record` reading old records. Enforced since 2026-08-20: `from_record` dispatches on `"v"` and *raises* on unknown versions, and `to_record` re-encodes each offer in its native version, so old records keep their ids (v5 since 2026-09-18/19: `requires` and a `Bond` deposit; a requirement or a deposit selects v5, everything else stays v4).
- **U3 Clearing trusts no solver.** `MockClearing.submit` asserts the proposal's ontology pin equals its own (step 0 — U10's rehearsal), re-derives every leg against the *current* book and its own ontology — `check_match` for a simple leg, `check_composition` for a composed one (the thing's give moved by an operator give, since 2026-09-13) — re-checks balance (node potentials exist; a simple cycle: product > 1), surplus and fills, and only then commits — all fills under one root (atomicity = one recordstore commit). Any future clearing backend keeps this shape.
- **U4 Solve against pinned roots.** Solvers work on `registry.snapshot()` (a frozen `RecordStore.at`) and pin `ontology_root`/`book_root` in proposals. Reproducibility and auditability beat freshness.
- **U5 Positive rates only.** The loop math is products of positive ratios / sums of logs. There are no negative prices anywhere: disposal is a positively-priced service, emptiness a positively-priced good (see ARCHITECTURE.md §7). Reject any change that introduces signed prices into `Match.rate`.
- **U6 Determinism in the baseline solver.** `ExchangeGraph.find_profitable_loop` iterates nodes/edges in sorted order; the same book must yield the same loop on every replica. Smarter solvers may be stochastic; the baseline may not.
- **U9 Exact rationals in everything clearing re-verifies.** (Entered 2026-09-14 with the v4 record; the id is the plan corpus's.) Quantities, amounts, rates, potentials and surplus are `Fraction`s on the clearing path (`schema.q`), records spell them `n/d` (`schema.rat`), and no epsilon survives in a gate; the solver's `-log` Bellman–Ford search stays floating point because a search is a heuristic and clearing is the truth. A float typed by a person is the decimal it prints as (`99.99` is 9999/100).
- **U7 Vocabulary fails closed.** Unknown categories never match (`Ontology.satisfies` is strict). Silent drift must break loudly.
- **U11 No partially-filled loop survives a merge unnoticed.** (Entered 2026-08-20; the id is fixed by the plan corpus, hence the gap after U7.) Post-merge, every `loop/` record holds a `fill/` for each of its legs — whole (`fill/<offer>`) or, for a divisible give taken in part, per loop (`fill/<offer>/<loop>`, since 2026-09-14) — every fill points at a present loop, no give is filled both whole and in part, and the partial fills of one give sum to at most its quantity (oversold); `verify_loop_atomicity` checks this on every reconciled commit and raises `PartialLoopError`. Checked, not resolved — evicting a cleared loop would be a finality rollback; the deterministic loop-granularity resolver is a registered open problem (`docs/plans/P1-federated-book.md` §3).

## Running tests

```bash
python3 -m pytest tests/ -v            # everything (conftest.py puts src/ on the path)
python3 -m pytest tests/test_boundaries.py -v   # B1/B2 — must always pass
PYTHONPATH=src python3 examples/demo_triangle.py  # end-to-end smoke: must find & clear 1 loop
LOOP_HOME=$(mktemp -d) PYTHONPATH=src python3 -m loopmarket --catalogue examples/triangle.od < examples/triangle.loop  # the same triangle as text
```

Installing deps: this environment's Python may be PEP-668 externally managed — use `pip install --break-system-packages` (or a venv). `pip install -e ".[test]"` for development; add `.[swarm]` only when running against a Bee node.

Live-node runs follow ontodag's convention: skip unless `BEE_API` **and** `BEE_BATCH` are set, and always pass a real purchased batch id so nothing auto-buys. The Swarm-book test additionally needs `BEE_SIGNER` (a throwaway key: it writes feeds under timestamped topics so reruns don't inherit an old book).

## Architecture map (one line per module)

- `src/loopmarket/schema.py` — the offer form (**v5 record, decided 2026-09-18 and accepted in its final shape 2026-09-19, `docs/plans/P3-release-and-reclearing.md` §5d — admissibility by declaration:** `Requires(point, ladder, accepts, oracles, escrows)` on every v5 offer — the maker's *neutral point* on a no-show on its own scale, the cancellation *ladder* over lead time (ordered `(lead seconds, amount)` points read linearly at the cancellation's lead before the leg's handover window, the CLI deriving it over the lead at posting in one of four shapes), the durable, escrowable asset categories it accepts as compensation each with its own price per unit (`Acceptance`), the witness types and escrow kinds accepted; and `Bond(asset, value, escrow)` on a give — a deposit (a `Thing`: category, quantity, unit) worth `value` on the giver's scale, held by an escrow contract, reserved per fill as bond × taken / quantity. A leg is admissible when the deposit's category falls under one the wanter accepts (the catalogue, one-way) in that entry's unit and the reserved quantity covers her point at her price for it — two conversions each inside one maker's scale, one comparison in the asset's unit, no asset named by the protocol, nothing converted after clearing (U14); a point with no acceptance is met by nothing (U7). A requirement or a deposit makes an offer v5; v4 re-encodes byte for byte; the escrow contract exists since 2026-09-19 (`escrow.py`) and with an escrow set the gate counts a deposit only up to what the contract holds): `TimeWindow` (open-ended allowed since v3), `Thing`, `Parts`, `Tokens`, `Offer` (+ `give`/`want` helpers; `ask`/`bid` kept as order-book synonyms), canonical encoding, content-addressed ids, `q`/`rat` (exact numbers, U9). **v4 record (2026-09-14, decided with Peter):** every number a reduced `n/d` string; `step` and `min` on a thing in place of `divisible` (`step` 0 continuous, the whole quantity indivisible, 1 whole apples, 25 sacks; `min` the give-side floor, the chartered bus); a want may be `Parts`, several things all or nothing under one price; `Thing.takes(qty)` is the one quantity rule. v1–v3 re-encode byte for byte. **v3 record (2026-09-12, `docs/plans/P1-spacetime-terms.md`):** no `service`/`where` fields — where and when a thing changes hands are role terms in `Thing.concepts`; `valid` may have no end; `GeoDisc` and the haversine survive only to read and match v1/v2 records among themselves (passing `service`/`where` to `give`/`want` yields a v2 record). No new disc is ever created.
- `src/loopmarket/spacetime.py` — geohash cells: `geohash`, `cell_bounds`, `cell_for_coords` (the finest cell containing a radius — what a bare `LAT,LON,R` becomes); input vocabulary only, ontodag's prefix kind orders the stored name. The day-bucket/cell-prefix chains retired with the `idx/` index (2026-09-12).
- `src/loopmarket/ontology.py` — the catalogue facade: `assert_edge`/`load` to build, `covers`/`satisfies` to query (`known` = a node, or a parametric term of a declared head the DAG can order — computed containment for `from(u2e4x) ⊑ from(u2e)`, and since ontodag #15 a bare place node or a role term naming one, `shop`, `ljubljana`, `my_home_4th`, `from(my_home)`, ordered by the graph; undeclared heads, malformed values and a name outside the dimension fail closed, U7), `persistent()`/`commit()` for pinned roots via `EagerOntoDAG`. **Three relations, declared in the catalogue (Peter, 2026-09-13).** Where and when a thing changes hands are *bare* geo and time terms in the conjunction — `give vegetable-box shop`, a cell, a region, a window, a named time; no `where`/`when` head (they lived one day, 2026-09-12, and were unnecessary). A head stays only for two coordinates of one kind (a route's `from`/`to`, a transport's `depart`/`arrive` — `declare_roles({head: base})`, one edge each) or for a descriptive geo/time term (`made_in`, `made`). **Handover coordinates match when one side contains the other**: the seller delivering anywhere in the city serves the want at the door, the shop serves the buyer collecting anywhere; both directions of `is_below`, never partial overlap. **Categories and descriptive terms match one-way**: the give fits within the want. The seed says which is which: `geo` and `time` hang under the marker `handover` (`declare_handover`; roles under them inherit it), a descriptive head opts out under `descriptive` (`declare_descriptive`) — the two names the core knows besides the dimensions'. **An operator's argument matches want-within-give** (Peter, 2026-09-13 evening: *the parameter of the transport is a want from the transporter's side*): `transport(small-item weight(..8kg))` on the courier's give is what the courier accepts, `transport(bicycle)` on the want is what the wanter hands over, and `bicycle ⊑ small-item` decides — the operator category itself (`transport`, under the marker `operator`, `declare_operator({"transport": ("from", "to")})`) goes the usual way; a conjunction in the argument is several constraints, `transport(A B)` ≡ `transport(A) transport(B)`, and the CLI stores every term in the catalogue's canonical spelling (`surface.elaborate`, U2); a give constraint the want does not answer refuses. One rule under all three: whoever fixes a value states a fact, whoever leaves it open an acceptance, and the fact lies within the acceptance. The operator term is ontodag's graph kind (#19, 0.26.1): `declare_operator` puts the category under `graph-dimension` as well as `operator`, ontodag canonicalises and orders the term, and a spelling it refuses (an unknown or redundant constraint) matches nothing. A head the want does not name constrains nothing; a coordinate the give does not state answers nothing (an internet service has no place). The 2026-09-12 `service-role` marker and its overlap relation ("a handover point exists") were the same idea read as ranges on both sides; partial overlap was the error, not the two directions (`docs/plans/P1-spacetime-terms.md` §3, superseded).
- `src/loopmarket/registry.py` — the book: `offer/`, `sig/`, `withdraw/`, `fill/`, `loop/` keyspace over a duck-typed RecordStore — **no index in the book**: the `idx/{c,t,g}` prefixes (maker books until 2026-08-21, aggregator-derived after, read by nothing throughout) retired 2026-09-12 with the one-query generator — ontodag is the one intersection engine, `docs/plans/ontodag-coupling.md` §5, and the manifest's `index_root` went with it; `snapshot()`, `withdraw()` tombstones, `attach_handoff`/`handoff`/`handoffs`/`loop_of` (the `handoff/` sidecar), `or_set_resolver` for multi-writer merges, `verify_loop_atomicity` (U11), `swarm_offer_book()`.
- `src/loopmarket/announce.py` — the announcement channel (2026-09-14): how a book becomes discoverable — `Announcement(owner, book, role)`, latest per owner, retractions; three backends behind one protocol, chosen by spec like books: `chain:RPC_URL@CONTRACT` (the `LoopBookRegistry` event log on the EVM chain Swarm settles on — Gnosis today; nothing chain-specific in contract or code; `contracts/LoopBookRegistry.sol`, ABI beside it, `scripts/deploy_registry.py`; **deployed 2026-09-14 on Gnosis at `0xD4379E494a488411D964BebDb210C0bf628d97af`**, first announcement read back through the chain the same day; web3 lazy behind the `chain` extra, B2), `file:PATH` (sessions on one machine), `memory:`; several comma-separated are one channel (`UnionAnnouncements`, the newer chain last) so a chain move is a setting. `msg.sender` is the owner, the feed-signing key, so the announcement authenticates the book (U8). GSOC dropped the same day (Peter): per-book announcements have no volume to optimise, the chain is what censorship is detected against, and a full-node receiver per aggregator was the wrong shape.
- `src/loopmarket/federation.py` — the federated book: `Aggregator` folds announced per-maker and clearing books under the U8 admission rules (rejections as attributed provenance, U11 per fold) into the three-root `Manifest` {book, provenance, announcement} (`index_root` dropped 2026-09-12 with the `idx/` index it named); `audit_manifest` is the T14 cross-audit — (announced set) − (speech under `book_root`) as absence proofs anyone verifies without a store; since 2026-09-14 with `expected=channel.announced()` a never-folded book is an omission too, so completeness is one reader's computation and aggregators are no longer trusted by agreeing with each other; `Aggregator.subscribe(channel, open_book)` folds exactly the announced set.
- `src/loopmarket/sigs.py` — detached maker signatures (U8's off-feed layer): sign/recover over offer ids (and `recover_public_key`, the key handoffs are sealed to), eth-keys loaded lazily behind the `sig` extra.
- `src/loopmarket/handoff.py` — sealed handoffs (2026-09-12, `docs/plans/P1-spacetime-terms.md` §4): the address is settlement text on the place node, never vocabulary or record; after clearing the place-owner's client seals it to the leg counterparty's public key (ECIES over the makers' secp256k1 keys) and writes `handoff/<loop>/<offer>` beside its own filled offer; the counterparty opens it from the fold. No side channel; the book is the channel. Fold admission: only beside an offer the book's owner made.
- `src/loopmarket/matching.py` — the exact pairwise check (`check_match`) and the O(gives×wants) baseline candidate generator; since 2026-09-18 every gate (simple, composed, parts, aggregated) refuses a leg unless each side's `requires` is met by the other side's declaration (`meets`: the witness type and escrow kind accepted, the deposit's category under an accepted one through the catalogue, the share reserved for this fill covering the point at the acceptance's price) — fail closed like vocabulary (U7), so no inadmissible leg is ever a candidate, the reserve bid honours requirements for free, and clearing refuses what a solver hand-builds.
- `src/loopmarket/graph.py` — `ExchangeGraph` (best rate per pair), Bellman–Ford negative cycles, `Loop` (product, surplus, per-node check, `loop_id`); **`enumerate_cycles` (2026-09-18, the §6 recall-gap fix, G1):** every simple cycle over the *whole* match multigraph up to `max_legs`, each judged on its own surplus and per-node feasibility, canonical order, a `complete` flag when the cap cut it — the candidates the packer chooses among; Bellman–Ford tops up only when the enumeration was cut.
- `src/loopmarket/selection.py` — loop selection (`docs/plans/P2-loop-selection.md`, 2026-09-18): `Item` (what a loop takes from each offer — a want whole, a give by the leg's quantity), `pack(items, capacity)` — the set worth most under the offers' capacities (an indivisible offer or a want once, a divisible give shared up to its remainder, U11 seen from the front), exact by depth-first branch and bound up to `EXACT_UP_TO` items within a deterministic node `BUDGET`, the `greedy` beyond, §8's total order (`order_key`: score, fewer legs, sorted keys); the objective Π(1+gain) exactly, or Σ (1−p)^legs·ln(1+gain) in fixed-precision decimal when a failure `prior` is given (uninformative, G3). One packer for the baseline and the beat.
- `src/loopmarket/clearing.py` — `LoopProposal`, `Receipt`, the `Clearing` protocol, `MockClearing` (injectable `clock` for deterministic tests), `ChainClearing` (2026-09-15: the same checklist, then the beat posted on chain via `beat.BeatClient`; the receipt's reason names the beat).
- `src/loopmarket/cli.py` (+ `__main__.py`) — the `loop` command line (built 2026-09-12, design `docs/plans/cli.md`): odag's settings table/config/REPL lifted, ontodag's grammar plus exactly two conventions (bare number first = quantity, last = price; a third, `+` between the parts of a composed want, want side only — `draft`/`drafts`/`compose`/`discard` and the `+` line parse, resolve and render today and refuse to publish until the v3 record carries parts, `cli.md` §13), every term the catalogue's (since the v3 record the same day, and since 2026-09-13 with no `where`/`when` head: a bare place name, cell, coordinate literal or time spelling is where/when the offer holds; the one interpreted head is `valid`, incl. `valid(A..)` until withdrawn; `set terms 'home ...'` adds default terms whose coordinate a line does not already state; the CLI knows kinds, never heads), places as catalogue nodes under their `geo(cell)` (`place`, the dated bridge; `LAT,LON,R` anywhere becomes the finest cell containing that radius), a *catalogue* name standing as spelled (`shop`, `ljubljana`, `my_home_4th`, `from(my_home)` — ontodag #15 orders the node by the graph) and a *private* place (a name only the personal store holds, which the pinned root cannot carry) as the term it hangs under (`geo(u24)`, the name stays private; a private region or floor is refused); `set terms home` — quantity terms still refused at publish (the field owns them), the price memory read from the book, the approval block = `show` renderer, bands accepted and refused at publish; `place ... [ADDRESS]`, `handoff ID TEXT`, `watch [--once]` (fills reported, handoffs sealed and opened) and `handoffs` since 2026-09-12. Tooling, not protocol: it encodes only what the schema holds. One private import from `ontodag.__main__` (`_open_catalogue`), pending a public opener.
- `contracts/` — Solidity: `LoopBookRegistry.sol` (the announcement channel, deployed on Gnosis) `TrieProofVerifier.sol` (2026-09-15: recordstore's trie proofs verified on the EVM, the P2 clearing contract's evidence layer — `tests/test_trie_verifier.py` on a local EVM, the `evm` extra; measured 1.2–1.6 M gas per inclusion, 0.64 M per absence; `docs/plans/proof-fabric.md` §1; **since 2026-09-18 under either addressing** — `addressing` 0 sha256 / 1 swarm, `SwarmAddress.sol` computing Swarm's chunk-tree address on the EVM: keccak of the little-endian span and the BMT root over 32-byte segments zero-padded to 4096, zero subtrees a constant per level, the 128-ary chunk tree above one chunk, verified against swarmfs's `content_address`; 57 k gas per 400-byte blob, 1.0–1.2 M per Swarm-addressed inclusion) and `LoopVerifier.sol` (the structural half of one leg: envelope and id, inclusion, v4 and pins, sides, quantities on step/floor/remainder, potentials by cross-multiplication; **since 2026-09-18 evening** the want's quantity and unit handed over by the thing's give or the aggregated gives together, and composed wants — one give per part, part i's quantity in part i's unit; ~3.7 M gas per one-give leg, 6.3 M per two-part composed leg; `tests/test_loop_verifier.py` on real proposals; which give among operators is the thing stays the semantic half's) and `BeatClearing.sol` (the optimistic beat: commitments plus a bond, a challenge window re-verifying one leg on chain, fills recorded at finalization so the chain is the fill authority, an arbiter hook for the semantic half; submit 0.57 M / challenge 3.7 M / finalize 0.28 M gas; `tests/test_beat_clearing.py`). `src/loopmarket/beat.py` is the Python side: `submission(proposal, snapshot)` builds exactly what the contract commits to (leg hashes over the ABI encoding, fills, potentials, pins — the fifth pin the root's addressing scheme, read from the proof envelope, since 2026-09-18) and `BeatClient` submits, challenges, finalizes and reads `filled`; `clearing.ChainClearing` is `MockClearing` plus the beat — the book keeps the data, the chain the commitments and, after `finalize`, the fills; a proposal the local checklist rejects never reaches the chain. CLI: the `beat` setting, `propose`, `beats [--open]`, `challenge BEAT [LEG] [--check] [--book SPEC]`, `finalize BEAT`. **Deployed 2026-09-15 on Gnosis at `0xFD1022636c2f0Cd3bbE5f4e40E0ee33C39654D08`** (bond 0.01 xDAI, window 720 blocks, arbiter the deployer's key); the live gate the same day posted the triangle as beat 1 from `loop propose` in five seconds. **Redeployed 2026-09-18 at `0x1277B4906b6Aab4dF806dd85c5ED980135C12931`** with the addressing pin, and that evening **at `0x6614D98e9659ED5f14DdD68c98C7e223a0CA47Bf`** with composed wants — the triangle posted from a Swarm clearing book as its beat 1 and verified from a fresh session the same hour — and, late that evening, **at `0x6699A442630356fcBB4E0DFbD67c2E6D5550F7Ea`** accepting v5 records and checking the declared requirement, then **at `0xe5699BE764CE66209b29D25c22413feffD63433A`** comparing the bond's share reserved per fill, and on 2026-09-19 **at `0x75025e88749963B85c95f2EFB0143D76eA7169B8`** reading the accepted v5 record's deposit and acceptance table (same parameters; `scripts/build_beat.py` rebuilds the shipped artifact; each address keeps its beats as history). **The challenger since 2026-09-18:** the CLI keeps no copy of what it posted (the book is the channel, as for handoffs) — `challenge` reads the beat, finds the `loop/` record behind it in the submitter's announced clearing book (or `--book`, or mine) by rebuilding the submission from the record and the snapshot at the beat's root and hashing to the commitments (`beat.find_evidence`/`proposal_from_record`/`commitment`), re-derives every leg off chain with `MockClearing.verify_leg` (factored out of `submit`; `rehearse` = the checklist without a commit) against what the snapshot and the chain leave of each give, asks the contract's verifier for free (`BeatClient.verdict`: `verifyLegExternal` via `eth_call` sent as the contract, funded by a state override where the node has one, a priced call within the bonds otherwise), and sends only for a leg the contract would convict; a fault only the off-chain check sees is reported as the arbiter's, a beat with no record anywhere as unverifiable (exit 2). **`ChainClearing` asks the verifier before paying the bond** (`verdict_of` on every leg, the same day): a beat the contract would convict is a rejection, never posted. **Live gate 2026-09-18:** beat 2 posted from a *Swarm* clearing book and challenged from a fresh session — evidence found through the Gnosis announcement and read from Swarm, legs holding off chain, and the contract answering "node hash mismatch": a Swarm-addressed book proves under BMT roots the sha256 verifier cannot check, so the honest beat was convictable and was cancelled on chain by the challenge. The BMT verifier was built the same day (above), the contract redeployed, and the gate rerun that evening: a fresh Swarm clearing book posted beat 1 on the new contract and the fresh session's `challenge 1` answered *beat 1 verifies* — every leg holding off chain and on. Composed wants verified on chain since that evening (`LoopVerifier`, redeployed), and the sealed-proposal beat in front of the contract the same evening (`SealedBeat.sol` at `0xbE1Af10c55dc9969539f5a1de79eD663bdaDDCd5`, `auction.py`). The P2 design (decided 2026-09-15): an optimistic beat contract verifying the structural half on chain — pins, inclusion, unfilled, quantities within step/floor/remainder, potentials by cross-multiplication — with fills recorded by the contract, and the semantic half (fits-within) optimistic: anyone re-derives a cleared leg off chain and a wrong match is challenged within a window.
- `src/loopmarket/auction.py` — the sealed-proposal beat (2026-09-18, `docs/plans/P2-batch-auction.md` §2–§6): a proposal is a bundle of loop records pinning one root, sealed as keccak(bytes || salt) and revealed on `SealedBeat` (`contracts/SealedBeat.sol`: fixed cadence, one commitment per solver per beat, reveals emitted, the outcome recorded once and a divergent record a `Disputed`); `outcome` re-derives every revealed loop with the clearing checklist (U3), adds the baseline's loops as the reserve bid (§8; a revealed loop outranks the reserve's copy), applies the fairness filter (§5: an offer's reference is the best gain any candidate through it offers, the measure today's uniform per-leg gain) and selects the offer-disjoint set with the highest numeraire-free score — the exact product of (1 + gain), Σ log surplus (U14) — exactly up to twelve survivors, greedily beyond, ties by loop_id then bundle hash (U6 extended to the beat); winners go to `BeatClearing` loop by loop through `ChainClearing` and the book records `auction/<beat>`. `SealedBeatClient` / `MemorySealedBeat` behind `open_sealed`; CLI `auction` setting, `commit`, `reveal`, `outcome`, `sealed`. **Deployed on Gnosis at `0xbE1Af10c55dc9969539f5a1de79eD663bdaDDCd5`** (240-block beats, 120 to commit; redeployed at `0x2014e7D3A097a709c3d27Df67617dcF9879A2A2A`, `0x79493a82F36B9EEABDE974044020F72055Bf4A32` and, 2026-09-19, `0xFB533254050087E384DEB98CAF2be4874Ba7c592` with the v5 clearing contracts) and **live the same evening**: commit, reveal, outcome and record on its third beat, the winner posted as a clearing beat and *verifies* from a fresh session — after two live runs surfaced (a) the fold must be computed under the book's addressing (`_fold_blobs`: a Swarm-addressed book folds in a scratch directory under Swarm addressing, so the pinned root is one the book and the chain resolve) and (b) the chain's fills are the authority everywhere the book's remainder is asked (`MockClearing(chain_fills=)`, `SolverAgent.chain_fills`, `auction.outcome(chain_fills=)`, the CLI passing `BeatClearing.filled` whenever `beat` is set). Not built: Shutter as the sealing (§3's primary), solver bonds (factbond), the spread leg (§7).
- `src/loopmarket/escrow.py` + `contracts/LoopEscrow.sol` — the crypto escrow (P3 §5a, built 2026-09-19; **custody here, adjudication in factbond** — §5e, Peter the same day: *a ruling or a timeout*, rulings for contested claims only, the claim mechanism factbond's and not a second copy): a smart contract as a maker — signs by state (`registerOffer`; a reader authenticates against `offers` instead of a signature, U8) and takes no personal tokens (its holding is a condition on the giver's give, never a leg). A giver's declared `Bond` is deposited behind the *offer id* (`deposit` in the chain's native coin, `depositToken` for an ERC-20); the clearing's key reserves a share per fill (`reserve`: bond × taken / quantity, §3a rule 8, with the leg's wanter, handover window, the *resolver* both offers declared acceptable, the claim period and the ladder in asset units). Every undisputed case settles without a ruling: `settle` after a quiet claim period (anyone; to the giver), `countersign` (the wanter's own act; to the giver now), `cancel` (the giver's own act; the ladder's amount at that lead to the wanter, `ladderAt` linear between points, the rest back). A contested claim is factbond's bonded assertion about (offer, loop): the resolver makes two calls, `hold` (the quiet timeout stops) and `resolve(toWanter)`, and can move that reservation only between that giver and that wanter — spelled by (offer, loop) for people and by the reservation's key alone (`hold(bytes32)`, `resolve(bytes32, uint256)`, the `subject` factbond's `Assertions` names; `EscrowClient.subject`). **factbond's contract exists since the same evening** (`../factbond/contracts/Assertions.sol`, custody here and adjudication there): `tests/test_escrow.py::test_factbond_as_the_resolver` compiles it from the sibling checkout and runs a claim on a real reservation both ways (certified by timeout, disputed and refuted); the CLI's `resolver` setting names it for the reservations `finalize` sends (a give's declared `arbitrator` wins; empty: my key rules directly). The giver leaves only with what no fill holds and only after a notice period (`notice`/`withdraw`, `free = held − reservedTotal`). **The chain is the authority on a deposit as on the fills (the same day):** `matching.meets(held=)` counts a deposit that names an escrow only up to what the contract holds (`escrow.held_units`; `MockClearing(escrow_held=)`, `SolverAgent.escrow_held`, the CLI passing it whenever `escrow` is set), so a declared, unfunded bond meets nothing; and **`loop finalize` reserves** — the fills becoming the chain's authority is when the deposits behind them lock per fill: `escrow.reservations_for` turns the beat's loop record (found as `challenge` finds it) into one reservation per bonded give — the share in smallest units, the wanter's key, the give's `arbitrator` or my key as resolver, the want's `time(...)` term as the window, `escrow_claim` (default 7d) as the claim period, the wanter's ladder converted at her acceptance price into the asset — and sends each with the clearing key. `BeatClearing` reserving from the contract itself is not built; the notice period guards the gap between clearing and finalization. Nothing is converted on chain: `escrow.to_wei` turns the exact quantity into smallest units and refuses one the asset cannot hold (U9). `EscrowClient` behind the `chain` extra, `scripts/deploy_escrow.py CLEARING NOTICE_BLOCKS`, the artifact from `scripts/build_beat.py`; CLI `set escrow chain:RPC@CONTRACT` (the record names the address only, so the id is RPC-free) and `deposit [ID] [--check]` funding each of my gives' bonds in the gas token. `tests/test_escrow.py` on a local EVM (native and ERC-20, the three undisputed paths, the resolver's bounds); the held gate and `reservations_for` in `tests/test_v5_record.py`. **Deployed 2026-09-19 on Gnosis at `0x7bee68244f2Bc2d67F21E5ae2eE7696Afca9c55F`** (clearing and stand-in resolver the deployer's key, notice 720 blocks) and **redeployed that evening at `0x299CE499fdDA61bCB006718E5Ac551B5006269Bf`** with the key-only calls (the first keeps beat 1's reservation); the live gate the same day: the bonded triangle found no loop while bruno's bond was declared and unfunded, `loop deposit` held 0.01 xDAI, the loop appeared, `propose` posted it as beat 1 on the current `BeatClearing`, and after the window `finalize 1` recorded the six fills and reserved the share (0.01 xDAI for the wanter's key, the clearing key as resolver, a 7-day claim period) — read back from a fresh client, held 0.01 and free 0: a bond held end to end.
- `src/loopmarket/solver/agent.py` — the baseline `SolverAgent`: snapshot → match → **candidates** (every simple cycle by `graph.enumerate_cycles`, since 2026-09-18; the circulation hunt over simple, operator-composed, parts and aggregated legs — `matching.composed_legs`/`parts_legs`/`aggregate_legs`, `graph.find_circulations`) → **select** (`selection.pack` under what is left of every offer, the chain's fills subtracted) → propose; `max_legs`, `cycle_limit`, `exact_up_to`, `pack_budget`, `failure_prior` are its knobs; the species other solvers must beat, and the beat's reserve bid. Smarter species live *outside* this repo by design: they propose, clearing verifies, and loopmarket never imports any of them.

## Known simplifications (deliberate; each has a roadmap home)

1. **Divisibility caveat.** The product condition (Π rates > 1 around a cycle) prices out exactly only when quantities can scale so per-node token balances cancel. Indivisible legs get the conservative `per_node_ok` gate in clearing. Proper clearing pricing (choosing actual prices between each give and want, distributing surplus) is P2 — now specified: equal log-surplus split under uniform directional clearing (`docs/plans/P2-clearing-pricing.md`); the divisible/indivisible line is the LP/ILP complexity boundary (`docs/plans/P2-loop-selection.md`) — **selection built 2026-09-18** (`selection.py`): one packing with per-offer capacities covers both sides while fills are the wants' own quantities, exact below N\*, greedy beyond, §8's order; the baseline and the beat select through it. **Composition decided 2026-09-07** (`P2-loop-selection.md` §10): one want may be satisfied by several unconditional gives — by operator along a dimension (transport moves place, storage moves time, exchange moves denomination) or by aggregation along quantity (six lifters) — the conjunction lives in the single want so all-or-nothing is free, and the buyer's divisible money give is split at clearing; give-side bundles stay excluded. **Declared parts decided 2026-09-12, built 2026-09-14 on the v4 record** (`P2-loop-selection.md` §10, `cli.md` §13): the buyer names the parts (`draft`/`+`/`offer` at the CLI, `Parts` in the API), the want publishes as one v4 offer, `check_parts` is its exact check, `parts_legs` the baseline search, and the fill names which give served which part with the quantity taken; no prices on parts, no cross-part constraints, drafts never enter the book. The give side gets a *floor*, never parts — the chartered bus, the cow: a minimum fill on one give, the mirror of aggregation, a flow lower bound (`Thing.min`, v4; the CLI spelling `10kg..` on a give is not wired yet); the kit is one indivisible thing; heterogeneous give bundles stay the shut door (the reseller is the route). **Granularity decided 2026-09-14:** `step` replaces the boolean. **Partial fills built the same day:** a fill takes exactly the want's quantity from a divisible give and the remainder stays open (`OfferRegistry.available`/`taken`/`loops_of`; matching takes `available=`, the agent passes `book.availability`, clearing re-derives against its own book's remainders), the fills of one give are keyed per loop and may not sum past its quantity (U11), and a remainder below the floor or one step is dust that exhausts the offer (`Thing.exhausted`; the U13 rule). So the stepped clearing regime is the exact one, (a): no rounding, ever — the LP with rounding is not needed while fills are the wants' own quantities. **Aggregation by quantity built the same day** (the six lifters): `Leg.quantities` carries an aggregated leg's shares, `check_aggregate` is its exact check (each share within what that give may give, the shares summing to the want's quantity), `aggregate_legs` the deterministic depth-first baseline search over the gives of the want's thing, wired into the agent beside the operator and parts searches; clearing re-derives the split, and the fill names every give with its share. **The baseline solver composes since 2026-09-13** (Peter: *the whole point of a solver is to find circulations*): `Leg` (one want, one or more gives), `check_composition` (the thing's give moved by an operator give — a category under `operator`, `declare_operator({"transport": ("from", "to")})`, whose argument the thing must fit (`Ontology.accepts`: the box goes with the small-item courier, the piano does not — the payload check, 2026-09-13 evening) and whose input end is comparable with the thing's coordinate, its output replacing it), `Circulation` (a set of legs in which every maker both gives and receives; feasibility = node potentials exist, the dual of §11; `surplus` compounds the uniform per-leg gain and equals `Loop.surplus` on a cycle; `loop_id` agrees with `Loop` there) and `find_circulations` (deterministic depth-first, after Bellman–Ford's simple cycles). Clearing re-derives composed legs; the `loop/` record's legs carry `gives` and the set its `potentials`. One hop, one operator per leg; `examples/delivery.loop` clears the grocer's box at the shop plus the courier's run to the door. The reseller form (courier wants the box at the shop, gives it at the door) also clears as a plain cycle and is the *maker's* choice; §10's reason to prefer composition stands (unconditional offers).
2. **Candidate generation is O(gives×wants) by default.** Correct and fine in memory. **The dimension-backed generator landed 2026-07-30** (`src/loopmarket/dimensions.py`): `DimensionIndex` + `candidate_matches_indexed`, recall-exact vs the baseline (tests/test_dimensions.py); the index is a derived deepcopy of the catalogue — never file offers into the shared catalogue, that would churn pinned roots. **The want's categories are the query (2026-09-12/13):** `candidates` is exactly one `get([line marker, *one-way terms], items_only=True)` — an operator term filed and queried as its category, its argument the exact check's — ontodag's planner orders the cones and loopmarket does no set arithmetic on the answer (`docs/plans/ontodag-coupling.md` §5); `items_only` is ontodag #14's surviving half. Handover coordinates are left to `check_match`: a give that *contains* the want's place (the city-wide seller) sits above the want's term, not in its cone, and loopmarket does not stitch a downward and an upward walk together — the ask upstream, when book sizes want place pruning, is a query term meaning "comparable to X" (`ontodag-coupling.md` §7). Gives are filed under exactly the terms they carry plus a private record-line marker (a disc is not a cell: `check_match` refuses v2×v3 pairs); the v1/v2 window and disc are fields the exact check gates, not terms. The day's detours — three queries intersected in Python, ontodag #14's `overlapping=` planner argument, a whole-space value for roles a give is silent on — are all withdrawn: overlap was a modelling error, not a missing operator. Suite 54 s → 11 s. The baseline remains the default in `SolverAgent` and the recall benchmark; wire the indexed generator into the solver when book sizes ask for it.
3. **Geo cells: the truth for v3, a hint for v2.** Since the v3 record (2026-09-12) a place *is* a cell, a place node under a cell, or a region node above cells, and containment on it is exact (the graph's, since ontodag #15 landed the same night: `ljubljana` stands in the offer as a bare term and the exact covering of a place is a region node whose covering is a lower bound — CONTRACT G2); `LAT,LON,R` input becomes the finest cell containing the radius, so a place near a cell edge names the coarser cell unless a region names its covering. Peter's covering-as-a-value (`where(u24m+u24q)`, no node, no name) is deferred upstream to the anonymity tripwire (ontodag DIMENSIONS.md §14). For v1/v2 records the disc is the truth. Neighbour-cell fan-out or S2 coverings: P1, only if profiling says so.
4. **Single shared book in the demo.** The decentralized shape is one book per maker (own feed, own signer), aggregated with `RecordStore.merge` under `or_set_resolver`. The key layout is already identical; the aggregator loop is P1 — now specified end-to-end (announcement channel, aggregator manifest, merge discipline incl. the fill-determinism and loop-granularity fixes, tombstones, postage economics) in `docs/plans/P1-federated-book.md`. Note the 2026-08-07 demotion: the *shared* Swarm book (one feed, many writers) is a dev/demo tool only — feed CAS is best-effort, so one-signer-per-feed is the safety model.
5. **`bond`, `oracle`, `arbitrator` are carried, not enforced — but since 2026-09-18 a maker's *requirement* of them is enforced.** They are in the canonical encoding from day one so ids don't churn when P3 lands (bond escrow, oracle adapters, arbitration hooks, bonded ontology assertions — `Ontology.assert_edge` already takes `bond=`); the v5 record's `requires` (a neutral point, a ladder, accepted assets with prices, witness types, escrow kinds) against a give's `Bond` deposit is a matching gate off chain and a structural check on chain (an acceptance equal to the deposit's category *by name* is checked there; subsumption is the semantic half's), so the declared values already decide admissibility; what P3's escrow adds is that a deposit is *held* — **`LoopEscrow` built 2026-09-19** (`escrow.py`): held, reserved per fill, settled by timeout or the parties' own acts, a contested claim resolved by the resolver fixed at clearing (factbond's assertion market once it exists — custody here, adjudication there, `P3-release-and-reclearing.md` §5e); and, the same day, the gate consults the contract's holding wherever an escrow is set (`meets(held=)`) and `loop finalize` reserves the share per fill — so a bond is *held* end to end; what remains is `BeatClearing` reserving from the contract itself and factbond as the resolver. **Who receives a slashed bond** (working doctrine from Peter's examples, 2026-09-18, `P3-guarantee-coupling.md` §3a): obligations stand and the failed leg is replaced by a payment; the wanter's required floor is liquidated damages covering its whole reliance (payments to innocent counterparties of the leg, the substitute, the inconvenience); adjudication off the top, the remainder back to the defaulter; several defaulters split the one loss by their shares; repair is the wanter's choice, funded by the payout as a bridge leg; claims are staked; a bond is reserved per fill; **what a bond is held in** (`P3-release-and-reclearing.md` §5–§5a): never a personal unit (internal, instantaneous), never an asset the protocol names — the wanter names durable, escrowable categories with her own prices, conversion once at clearing on private scales; the escrow is a service, medium term a smart contract as a maker (signs by state, takes no personal tokens, its holding a condition not a leg), physical escrow at the roadmap's end; the v5 `bond`/`requires` shape was accepted 2026-09-19 and built the same day; a cancellation before the window owes the `cancel` floor and the solver repairs first, the wanter told only when no quick fix exists and free to refuse the offered one (§3a rules 8–10). **The P3 mechanism design now has a dedicated sister repo (2026-08-01): factbond** (github.com/petfold/factbond — bonded assertions/optimistic adjudication, odds-weighted bonds, shared bond pool, information insurance). Adopt, don't redesign: the loopmarket coupling (clearing-attached insurance on the ⊑ edges a loop relied on; per-edge loss experience as the catalogue reliability audit; certificates clearing the structural half of disputes) is worked out in factbond's `docs/INTEGRATION.md` §8 and ARCHITECTURE.md §4's update note here. **Decided (Peter, 2026-09-18): admissibility by declaration** — bonds enter selection as a maker's declared counterparty requirement (the v5 record's `requires`), a matching gate and an on-chain check rather than a weight; `docs/plans/P2-loop-selection.md` §4a records the alternatives, and `selection.weight(factor=)` stays as the hook for a risk weight nobody sets.
6. **Batch auctions: the sealed beat since 2026-09-18.** `MockClearing` is first-valid-wins locally; since 2026-09-15 `ChainClearing` posts each accepted loop as one optimistic beat on `BeatClearing`; since 2026-09-18 the sealed-proposal beat sits in front of it (`SealedBeat.sol`, `auction.py`: commit-reveal on a fixed cadence, numeraire-free scoring, the fairness filter, the baseline as reserve bid, deterministic selection — `docs/plans/P2-batch-auction.md` §2–§6). Not built there: Shutter as the sealing, solver bonds, the endogenous spread leg (§7; the fee-funded rewards were struck 2026-08-21).
7. **No privacy layer.** Offers are plaintext. Coarse-first disclosure, commitments, ZK fits-within proofs: P4 — now staged in `docs/plans/P4-privacy.md`; its Tier 1 needs no new cryptography, and its format-freeze list constrains P2 record formats. Do not add ad-hoc encryption before that design lands.

## Roadmap phases

- **P0 (this repo, done)** — in-memory prototype: uniform schema, book over recordstore, exact matching, negative-cycle solver, trust-nothing mock clearing, triangle demo green.
- **P1 Swarm book** — ~~run the demo against a Bee node (`swarm_offer_book`)~~ **done 2026-08-01**, permanent as the gated `tests/test_swarm_book.py` (needs `BEE_API`+`BEE_BATCH`+`BEE_SIGNER`): catalogue on Swarm, offers pinning its root end-to-end, book head in a signed feed, triangle solved and cleared in ~51s on a Gnosis-mainnet light node, a scorched-earth follower reading the cleared loop and all six atomic fills back from the network, second pass empty. The v2 record bump and its gates landed 2026-08-20 — version dispatch, widened pins with the `''`-gate closed, fill determinism, leg-paired loop ids, U8 sidecar primitives, the U11 stopgap checker; the P1 plan's code unblockers are cleared. The in-memory federation landed 2026-08-21: withdrawal tombstones, per-maker books folded by `Aggregator` under the U8 admission rules, the manifest (incl. `announcement_root`; its `index_root` and the `idx/` index it named are gone since 2026-09-12), and memory-backed variants of the convergence/follower/withdrawal gates. The T14 investigation's first code landed 2026-09-04: `audit_manifest` (omission as absence proofs from a manifest alone, tombstones included) and the solver-self-fold pattern (a solver folds the announced maker books itself and lands on the honest `book_root`; manifests are caches, never authority) — both in `examples/demo_federation.py`, which since the same day adopts ontodag's `core` pack (4,157 categories, ontodag>=0.19; core's `service` is the *financial* sense, so trades hang from a local layer under `lesson`/`work`/`produce`). **The read path decided and built 2026-09-14**: the registry event on Gnosis is the one announcement channel (GSOC dropped), `loop announce`/`announced`/`fold` and the `registry` setting, and with a registry set every session's fold is the announced set folded under U8 as each book's owner — the solver-self-fold as the default, manifests as caches audited against the chain's set. Still open in P1: the live-Swarm variants (per-maker feeds against a deployed registry, read-latency + durability gates); spacetime as ontodag parametric dimension terms in the *shared* catalogue (the derived DimensionIndex landed 2026-07-30 — see ARCHITECTURE.md §3; **work package `docs/plans/P1-spacetime-terms.md`, 2026-09-12 — built the same day through step 5**: the role-head rule in `Ontology.satisfies`, the role-aware `DimensionIndex`, and the v3 record — `service`/`where` gone, `valid` may be open-ended, v2×v3 pairs refused, the CLI interpreting only `valid`; cells are the exact geo truth; **and the three upstream asks landed the same night** — ontodag #15 (role heads take nodes: names stand in role terms, regions and floors match by the graph), #16 (`overlaps`/`meet`: `satisfies`' overlap half is ontodag's), #14 (overlap terms in `get` + `items_only`: `candidates` is one call, the `idx/{c,t,g}` index retired) — and, the same night, the overlap relation itself withdrawn, ontodag's overlap query mode gone, #18's dimension cache kept; **2026-09-13, from the vegetable-box example**: the `where`/`when` heads gone (bare geo/time terms), handover coordinates matching when one side contains the other under the `handover`/`descriptive` markers, `examples/delivery.loop` and `tests/test_handover.py` pinning the case and the P2 gap — and, the same day, the circulation solver: the shop's box plus the courier's run clear as one composed leg); the rest of the read-path decentralization investigation (T14: cross-audit as a running tripwire, solver-side folding as the default read path); ~~the command line~~ **built 2026-09-12** (`loop` / `python -m loopmarket`, `src/loopmarket/cli.py`, gates G1–G6 in `tests/test_cli.py`, the triangle as `examples/triangle.loop`; Peter's rulings the same day: the binary is `loop`, and a reused price in a batch under `confirm auto` refuses the line; **live-checked the same day** on a Bee 2.8.2 light node: `loop -f swarm:TOPIC < examples/triangle.loop` published, solved and cleared in ~2 min — seven feed commits — and a fresh session read the six filled offers and the same root back; the two gated suites passed the same evening in 9 min total, slower than the 51s/96s first recorded, feed lookups dominating; re-run the next morning after the draft/`+`/`offer`/`clearing` work: suites 5m38s, the triangle script 58s with the identical `loop_id` and book root — a fixed `now` makes the live book byte-reproducible; **0.3.0 live-checked 2026-09-12 evening**: both gated suites on the v3 record 4m41s, the triangle script on a fresh feed 1m51s with the in-memory G1 `loop_id`, a fresh session reading six fills back in 11s, the federation demo live with the core pack 4m25s, every check true). Still open there: live-Swarm announced books folded from a second machine (the 2026-09-14 gate ran maker and reader on one), and the three upstream asks in `cli.md` §11 (coordinate input for `geo`, relative time in `time(...)`, a public store opener) each of which deletes a loopmarket-only behaviour.
- **P2 Verifiable clearing** — a contract on the EVM chain Swarm settles on (Gnosis today) verifies offer inclusion/absence under the pinned book root via recordstore's shipped canonical-trie proofs (`prove`/`verify_proof`, ≥ 0.16.0); POT ForkPathProof is the conditional fallback only if the on-chain verifier demands BMT-native proofs (decision rule in `docs/plans/proof-fabric.md`); batch auctions over competing sealed proposals (`docs/plans/P2-batch-auction.md`); real clearing pricing (`docs/plans/P2-clearing-pricing.md`); loop selection as flow-LP/packing-ILP, reframed 2026-09-07 as circulation selection in a generalized flow network — cycle cancelling as the next solver species, clearing prices as node potentials, verification by potentials in O(legs) (`docs/plans/P2-loop-selection.md` §11) — **the potentials and composed legs are in the baseline since 2026-09-13; the v4 record with parts, per-fill quantities, `step`/`min` and U9's rationals since 2026-09-14, partial fills and aggregation by quantity the same day; and the contracts since 2026-09-15** — `TrieProofVerifier`, `LoopVerifier` and the optimistic `BeatClearing` (deployed on Gnosis at `0xFD1022636c2f0Cd3bbE5f4e40E0ee33C39654D08`, beat 1 posted live from `loop propose`; redeployed 2026-09-18 at `0x1277B4906b6Aab4dF806dd85c5ED980135C12931` with the Swarm-addressing pin and at `0x6614D98e9659ED5f14DdD68c98C7e223a0CA47Bf` with composed wants), the structural half verified on chain and the semantic half optimistic with an arbiter hook, fills recorded on chain at finalization. The `challenge` verb, the BMT/keccak verifier for books on Swarm, composed wants on chain and the sealed-proposal beat (commit-reveal; `SealedBeat.sol`, `auction.py`, §2–§6 of the batch-auction plan) landed 2026-09-18. Loop selection landed the same evening (`selection.py`, `enumerate_cycles`; G1 met). Remaining in P2: Shutter as the sealing, solver bonds and the spread leg, chains and netting (G4/G5), priors from data (G3).
- **P3 Guarantee fabric** — bond escrow and slashing, oracle adapters (countersign, locker, photo, digital proof), arbitrator hooks, bonded ontology assertions, aggregated risk markets feeding rate premia. Design home since 2026-08-01: the **factbond** sister repo (see Known simplifications #5).
- **P4 Privacy** — staged disclosure, committed offers, ZK fits-within/range proofs, private matching experiments.
- **Later (post-P4) — price and capacity schedules in offers** (owner-added 2026-08-21): the offer's single price generalizes to a static, immutable schedule over quantity (supply/demand curves) and/or validity time (a pre-committed Dutch/English ladder in one offer — no oversell, unlike a ladder of short-validity offers); the same record family covers **capacity/slot schedules** (k seats, one lesson per non-overlapping hour), with fills becoming slot/quantity-parameterized — until then the working forms are one-offer-per-slot/seat (strict today) and P2's qty-as-flow-capacity partial fills. Divisible-leg clearing stays a polynomial flow-LP under piecewise-linear convex costs; details and the standing rejections (continuous in-protocol price dynamics: speed races, U4/U6) in `docs/plans/P2-loop-selection.md` open problems. Until then, dynamic pricing quantizes to the beat: tombstone + repost.

## Conventions

- `src/` layout; tests via pytest from the repo root (conftest handles the path).
- Names are identity at public boundaries (strings accepted where objects are), matching ontodag.
- Docstrings explain *why* (the design decision), comments explain *why not* (the rejected alternative). Keep both current when changing behaviour.
- One invariant, one commit — mirror ontodag's fix history style.
- **Vocabulary (2026-09-07): loop, cycle, circulation.** A *loop* is any cleared circulation — the essay's word and the name in code (`Loop`, `LoopProposal`, `loop/`, `loop_id`). A *cycle* (or simple loop) is the strict circle, which is all the P0 solver can find and all today's `Loop` can hold; use it wherever the reasoning depends on going round (products around it, negative cycles, cycle packing). *Circulation* is the technical term when the flow theory is invoked (`docs/plans/P2-loop-selection.md` §11). Audit of the corpus done 2026-09-07; new text follows the rule.
- **Vocabulary (renamed 2026-09-07): clearing ≠ settlement.** *Clearing* is the atomic commit that turns a solver's proposal into fixed obligations (`clearing.py`, `MockClearing`, U3). *Settlement* is the makers actually delivering — P3's oracles, bonds and factbond. *Commit* is only the recordstore/chain write. The `loopmarket.settlement` import alias, kept one release, was removed 2026-09-14. `P2-clearing-pricing.md` (ex `P2-settlement-pricing.md`) follows the rename in full; the rest of the plan corpus under `docs/plans/` and `ARCHITECTURE.md` predate it and still say "settlement" for the clearing step — read them with that mapping, and use the new words in anything you write. factbond and ontodag's docs were updated the same day.

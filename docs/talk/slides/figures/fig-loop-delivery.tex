\input{preamble}
\begin{document}
\begin{tikzpicture}
  % Composition on the want side: every give is unconditional and names one
  % plain thing (the courier sells carriage, not toothbrushes); the
  % conjunction lives in the traveller's single want; the money give is
  % divisible and clearing splits it. Not a cycle: a balanced flow with a hub.
  \node[maker, align=center] (T) at (0,0) {traveller\\{\footnotesize wants toothbrush @ reception by 00:35}\\{\footnotesize gives up to 12 stablecoin (divisible)}};
  \node[maker, align=center] (S) at (-58mm,-34mm) {petrol station\\{\footnotesize gives toothbrush @ forecourt \hfill 3}};
  \node[maker, align=center] (C) at (58mm,-34mm) {courier\\{\footnotesize gives transport, forecourt $\to$ reception,}\\{\footnotesize 00:15 $\to$ 00:35 \hfill 9}};
  \draw[flow] (T.west) to[bend right=35] node[lbl, sloped, above] {stablecoin 3} (S.north);
  \draw[flow] (T.east) to[bend left=35] node[lbl, sloped, above] {stablecoin 9} (C.north);
  \draw[strong] (S.north east) -- node[lbl, sloped, below, pos=0.5] {toothbrush @ forecourt} (T.south west);
  \draw[strong] (C.north west) -- node[lbl, sloped, below, pos=0.5] {transport} (T.south east);
  \node[note, text=accent, font=\footnotesize] at (0,-42mm) {brush @ forecourt $\otimes$ transport $\;\sqsubseteq\;$ brush @ reception};
  \node[note, text width=118mm] at (0,-56mm) {Every give is unconditional and names one plain thing; the courier sells carriage, not toothbrushes. The conjunction lives in the traveller's \emph{one} want, so all-or-nothing is free. The traveller pays once: the money give is divisible and clearing splits it at the per-leg prices. Not a cycle: a balanced flow with a hub. Every node nets to zero on its own scale.};
\end{tikzpicture}
\end{document}
